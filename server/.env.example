PORT=
DATABASE_URL=
JWT_SECRET=
NODE_ENV=

# Email of the shared student account behind the "Try now" button. Leave empty to disable the demo.
# Create the account once with: pnpm --filter server demo-user
DEMO_USER_EMAIL=

# Email
GMAIL_USER=
GMAIL_APP_PASSWORD=
